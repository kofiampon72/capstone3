upstream dumbbudget_app {
    server localhost:3000;
}

# Main app server block
server {
    listen 80;
    server_name _;    # Accepts any hostname

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Proxy all traffic to the DumbBudget container
    location / {
        proxy_pass http://dumbbudget_app;
        proxy_http_version 1.1;

        # These headers tell the app the original request details
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support (if needed)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_cache_bypass $http_upgrade;

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Health check endpoint — useful for load balancers and monitoring
    location /nginx-health {
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}
